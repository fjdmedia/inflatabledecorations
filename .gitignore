# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Node
node_modules/

# Local server / build artifacts
.cache/
dist/

# Local-only
_duplicates/
Assets/Gallery/_duplicates/

# Internal client docs / pitch / test — NEVER publish on the public site.
# (Moved out to "Desktop/FJMedia Client Docs/Inflatable Decorations" on 2026-06-06.)
Assets/Docs/
Pitch/
Test Run/

# Local-only working folders (super-saiyan archives + delete staging + QA artifacts)
Archive/
delete/
.qa/
.qa-check/
Test Run/*.png
Test Run/node_modules/

# Python build artifacts
__pycache__/
*.pyc

# iPhone originals — converted to .jpg for the web and never committed.
# They are 2-3MB each and no browser renders them. A `git add -A` swept 46 of
# them into the index on 2026-08-16; this is the guard against a repeat.
# Method: Memory/reference/heic_to_web_conversion.md
*.HEIC
*.heic
